[project]
name = "weather-forecaster"
version = "0.1.0"
description = "AI-powered weather and business strategy dashboard"
readme = "README.md"
requires-python = ">=3.11, <3.13"  
dependencies = [
    "streamlit",
    "pandas",
    "openai",
    "requests",
    "python-dotenv",
]